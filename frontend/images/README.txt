This project generates all product images as inline SVG data URIs
(see skImg() in js/products.js) so the demo works instantly with no
image files to manage. Drop real image files in this folder and
point a product's "image" field at "images/yourfile.jpg" if you'd
like to use real photos instead.
